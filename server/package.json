{
  "name": "paper-hearts-server",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "start": "bun run migrate.ts && bun run index.ts",
    "migrate": "bun run migrate.ts",
    "test": "DATABASE_URL=postgres://localhost/paperhearts bun test --timeout 15000"
  },
  "dependencies": {
    "libsodium-wrappers-sumo": "^0.8.2",
    "postgres": "^3.4.5",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/libsodium-wrappers-sumo": "^0.8.2"
  }
}
