// Tests call handleApi directly — no subprocess needed.
// startServer/stopServer are kept for API compatibility with existing test files.

export async function startServer() {}
export async function stopServer() {}
