{
  "name": "paper-hearts",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev:client": "bun run --cwd client dev",
    "dev": "bun run dev:client & bun run start",
    "build": "bun run --cwd client build",
    "start": "bun run  --cwd server start",
    "deploy": "bun run start",
    "migrate": "bun run --cwd server migrate"
  },
  "dependencies": {
    "build": "^0.1.4",
    "run": "^1.5.0"
  }
}
